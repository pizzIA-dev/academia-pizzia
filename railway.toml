[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --no-input && python manage.py collectstatic --no-input && gunicorn educlass.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file -"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
